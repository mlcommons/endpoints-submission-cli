[build-system]
requires = ["setuptools>=80", "setuptools-scm>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "endpoints-submission-cli"
dynamic = ["version"]
description = "CLI tool for managing MLPerf endpoint submissions"
readme = "README.md"
requires-python = ">=3.10"
license = "Apache-2.0"
authors = [{ name = "MLCommons", email = "arav@mlcommons.org" }]
dependencies = [
    "click>=8.1",
    "httpx>=0.25",
    "pydantic>=2.0",
    "rich>=13.0",
    "pyyaml>=6.0",
]

[project.scripts]
endpoints-submission-cli = "endpoints_submission_cli.main:main"

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "pytest-mock>=3.0",
    "ruff>=0.4",
    "mypy>=1.10",
    "types-PyYAML",
]
docs = [
    "sphinx>=7.0",
    "sphinx-autodoc-typehints>=2.0",
    "furo>=2024.1",
]

[tool.setuptools_scm]

[tool.setuptools]
packages = [
    "endpoints_submission_cli",
    "endpoints_submission_cli.commands",
    "endpoints_submission_cli.commands.runs",
    "endpoints_submission_cli.commands.submissions",
    "endpoints_submission_cli.runs",
    "endpoints_submission_cli.submissions",
    "submission_checker",
    "submission_checker.models",
    "submission_checker.models.aggregate",
    "submission_checker.models.file",
]

[tool.setuptools.package-dir]
"" = "src"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=src --cov-report=term-missing -v"
markers = [
    "unit: fast, isolated unit tests (no network, no subprocesses)",
    "integration: tests that require a running API at http://localhost:8080",
]

[tool.coverage.run]
source = ["src"]

[tool.coverage.report]
show_missing = true

[tool.mypy]
python_version = "3.10"
strict = true
warn_unreachable = true
files = ["src"]

[[tool.mypy.overrides]]
module = ["yaml.*"]
ignore_missing_imports = true

[tool.ruff]
src = ["src"]
line-length = 100

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes (unused imports, undefined names)
    "I",   # isort
    "UP",  # pyupgrade (modern Python syntax)
    "B",   # flake8-bugbear (likely bugs / bad practices)
    "C4",  # flake8-comprehensions (unnecessary comprehension patterns)
    "SIM", # flake8-simplify
    "D",   # pydocstyle (Google-style docstrings)
]
ignore = [
    "SIM108", # ternary operator — sometimes less readable
    "D105",   # magic methods don't need docstrings
    "D107",   # __init__ docstrings belong on the class
]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.lint.isort]
known-first-party = ["submission_checker"]

[tool.ruff.lint.per-file-ignores]
"tests/**" = [
    "TCH",    # test files don't need TYPE_CHECKING optimisation
    "E501",   # long patch() target strings are unavoidable in test files
    "SIM117", # deeply-nested with-patch blocks are intentional for clarity
    "D",      # docstrings not required in test files
]

[tool.mutmut]
also_copy = ["test_submissions/"]

[dependency-groups]
dev = [
    "hypothesis>=6.155.3",
    "mutmut>=3.6.0",
]
